import React from "react";
import { View, Text, StyleSheet } from "react-native";

const DesktopBackground = () => {
  return (
    <View>
      <Text>Desktop Background</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default DesktopBackground;
